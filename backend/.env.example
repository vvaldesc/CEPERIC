# Database Configuration (Cloud SQL)
# Dejar DB_HOST vacío para desarrollo sin base de datos
DB_HOST=
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=ceperic_db

# Para conectar a Cloud SQL:
# Opción 1: IP pública
# DB_HOST=34.175.xxx.xxx
# DB_PASSWORD=tu_password

# Opción 2: Cloud SQL Proxy
# DB_HOST=host.docker.internal

# Firebase Configuration
FIREBASE_PROJECT_ID=ceperic-68bcd
FIREBASE_CREDENTIALS=

# Application Configuration
ENVIRONMENT=development
PORT=8080
ALLOWED_ORIGINS=http://localhost:4200,https://ceperic.web.app

# Cloud SQL Connection (para producción en Cloud Run)
# INSTANCE_CONNECTION_NAME=ceperic-68bcd:europe-west1:ceperic-db
# DB_HOST=/cloudsql/ceperic-68bcd:europe-west1:ceperic-db
